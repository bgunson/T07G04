In order to run the junit tests, go into command promp and change the directory to that of gui, SG and then src and 
compile then run the tests, which test both the board and ship classes for errors.
